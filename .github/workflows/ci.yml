name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  lint-validate:
    name: Lint & Validate
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: ShellCheck
        uses: ludeeus/action-shellcheck@2.0.0
        with:
          scandir: "./scripts"
          severity: warning

      - name: Validate YAML configs
        run: |
          pip install yamllint --quiet
          yamllint -d relaxed config/

      - name: Validate state schema
        run: |
          python3 -c "
          import yaml, sys
          files = [
            'state/progress-schema.yaml',
            'config/compliance/blocklist.yaml',
            'config/compliance/policy-rules.yaml',
          ]
          errors = []
          for f in files:
            try:
              data = yaml.safe_load(open(f))
              print(f'OK: {f}')
            except FileNotFoundError:
              print(f'SKIP (not found): {f}')
            except yaml.YAMLError as e:
              errors.append(f'{f}: {e}')
          if errors:
            print('YAML errors:', errors, file=sys.stderr)
            sys.exit(1)
          "

      - name: Validate compliance config structure
        run: |
          python3 -c "
          import yaml, sys
          checks = {
            'config/compliance/blocklist.yaml': dict,
            'config/compliance/policy-rules.yaml': dict,
          }
          errors = []
          for f, expected_type in checks.items():
            try:
              data = yaml.safe_load(open(f))
              if not isinstance(data, expected_type):
                errors.append(f'{f}: expected {expected_type.__name__}, got {type(data).__name__}')
              else:
                print(f'OK: {f}')
            except FileNotFoundError:
              print(f'SKIP (not found): {f}')
          if errors:
            for e in errors: print(e, file=sys.stderr)
            sys.exit(1)
          "

  bats-tests:
    name: Bats Regression Tests
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install bats-core
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq bats

      - name: Run api-caller.bats
        run: bats tests/api-caller.bats

      - name: Run concat-episode.bats
        run: bats tests/concat-episode.bats

  secret-scan:
    name: Secret Scan
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Scan for leaked secrets
        uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  compliance-config-test:
    name: Compliance Config Test
    runs-on: ubuntu-latest
    # 仅在 push to main 时运行（PR 跳过，避免暴露 key）
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: [lint-validate, secret-scan]

    steps:
      - uses: actions/checkout@v4

      - name: Check env vars present
        env:
          ARK_API_KEY: ${{ secrets.ARK_API_KEY }}
          IMAGE_GEN_API_URL: ${{ secrets.IMAGE_GEN_API_URL }}
          IMAGE_GEN_API_KEY: ${{ secrets.IMAGE_GEN_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: ./scripts/api-caller.sh env-check